# GPU commands

Create a virtual environment for GPU usage:
```bash
./GPU/create_venv.sh
```

Start the virtual environment:
```bash
source GPU/.venv/bin/activate
```

Install requirements:
```bash
pip install -r GPU/requirements.txt
```